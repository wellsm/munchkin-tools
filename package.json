{
  "name": "munchkin-tools",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@munchkin-tools/web",
    "build": "npm run build --workspace=@munchkin-tools/web",
    "test": "npm run test --workspace=@munchkin-tools/web",
    "lint": "npm run lint --workspace=@munchkin-tools/web"
  }
}
