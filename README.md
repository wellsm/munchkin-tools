# Munchkin Tools

Mobile-first web companion for Munchkin sessions. Tracks your heroes — name, gender, level, gear, classes, races — and computes strength vs monster in combat.

## Development

    npm install
    npm run dev       # Vite dev server
    npm run test      # Vitest
    npm run build     # production build
